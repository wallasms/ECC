# Claude Code context

Trabalhe no Agent Command Center preservando arquitetura local-first e contratos `/api/*`. Rode `rtk npm test` e `rtk npm run check`. O frontend atual segue a direção Quiet Command e está aberto para refinamento visual; não enfraqueça redaction, bind localhost ou scanners read-only. Use os agentes e skills em `templates/` apenas como modelos: nunca instale hooks sem revisão manual.
